Cursor
EXECUTION GATE: Allowed Source registry ONLY | Unknown API BLOCK | Missing info ASK ONLY | No inference allowed


Antigravity
[AG TASK | B42 STRICT | REGISTRY AWARE]

質問、アイコンを作るとしたらPXはどれくらいのものを用意すべき？


アイコンのサイズ（PX）について、Project Zomboid（特に Build 42 の 4K 対応）を考慮した推奨基準をお答えします。

推奨アイコンサイズ
HUD用アイコン（変異度計の左側など）

64 x 64 px（推奨）
理由: バニラの多くは 32x32 ですが、以前のご要望で「4K 対応」を重視されているため、2倍の解像度である 64x64 で作成し、コード側で適切なサイズに縮小して描画するのが最も「くっきり」と綺麗に見えます。
インベントリアイテム用アイコン

32 x 32 px (バニラ標準)
64 x 64 px (B42 高解像度推奨)
理由: B42 では 3D モデルからの自動生成も増えていますが、2D アイコンを自作される場合は 64x64 で用意しておくと、将来的な 4K モニターでの視認性が格段に向上します。
作成時のコツ
マスター画像: 制作時は 256x256 または 512x512 の大きなサイズで描き、最終的に 64x64（HUD用）や 32x32 に書き出す（ダウンスケールする）のが一般的です。
背景: 必ず 背景透過 (PNG) で作成してください。
デザイン: PZ の HUD は暗色ベースなので、アイコンは少し明るめの色使い（あるいは白・発光系）にすると、今回実装した毒赤色などの背景チップの上でも映えます。
もし「こんな感じのアイコンが欲しい」というイメージがあれば、私が generate_image を使って 64x64 サイズのベース素材を作成することも可能です。必要であればお申し付けください。

アイコンの色も変える場合、できるか？
はい、技術的に可能です。 しかも、変異度に合わせて「動的に」色を変えることができます。

1. 「白い」アイコンを用意する（推奨）
アイコン画像をフルカラーではなく、あえて「白、あるいはグレー」で作成します。

2. drawTextureCol を使う
PZ の Lua には drawTextureCol という関数があります。これを使うと、コード側で指定した RGB 色（例：水色や赤色）をアイコンの上に重ねることができます。

メリット: 画像ファイルを 1 つ用意するだけで、変異度 0% なら水色、100% なら毒赤色……と、ゲージの色と完全に同期させて色を変えることができます。
実装例: 現在、バーの色を計算している変数（r, g, b）をそのままアイコンの描画関数に渡すだけで、同期が完了します。
もしアイコンの色を「常に固定」にするのではなく、ステータスの危険度（Clean/Danger等）に連動させてアイコン自体も変色させたい場合は、この手法が最もスマートかつ premium な見た目になります。

ぜひ検討してみてください！