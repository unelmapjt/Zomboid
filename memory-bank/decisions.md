# Decision Log — NOX: EVOLVED (B42)

> 本ファイルは、プロジェクト上で行われた**技術的意思決定の根拠と経緯**を記録する。
> 「なぜそのアーキテクチャを選んだのか」のトレーサビリティを保持する。

---

## 決定ログフォーマット

```
### [YYYY-MM-DD] 決定タイトル
- 状況: どのような問題・状況が発生したか
- 判断: 何を選択したか
- 理由: なぜその選択をしたか（Rule-G/E への参照を含める）
- 結果: 実施後の評価
```

---

## 決定ログ

### [2026-04-21] B42 カメラズーム制御の廃止
- 状況: プロローグ開始時の演出として、ズームイン・アウトを実装しようとした。`setZoom`・`IsoCamera.zoom直接代入`・`FBO直接操作`・`doZoomScroll連打` など複数の独立したアプローチが全て失敗した。
- 判断: カメラズームを演出から完全に削除し、ダイアログのみのシンプルな開始シーケンスに変更した。
- 理由: B42 カメラは「Continuous Solver（連続的な解決プロセス）」であり、スクリプト層からの直接制御は設計上不可能（Rule-G9 適用）。複数の独立したアプローチが全て失敗したことで「Engine-authority limitation」と再定義。
- 結果: エラーなし。安定した起動シーケンスを実現。

### [2026-04-21] プロジェクト統治アーキテクチャの再設計
- 状況: `.cursorrules` `.antigravityrules` `.cursor/rules/` `memory-bank/` が並立し、AI の判断フローが分散していた。
- 判断: 1軸化（`.cursorrules` を唯一のトップ）・Decision Flow 明文化・`registry/` 正式昇格・`memory-bank/` の状態/履歴分離を実施。
- 理由: 「意思決定の単一ルートがない」という本質的問題の解消。制御レイヤーの空白を防ぐため、`.antigravityrules` は即削除せずに deprecated 扱いとした。
- 結果: 実行中。
